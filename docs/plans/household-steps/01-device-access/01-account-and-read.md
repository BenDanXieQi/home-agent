# 任务 1.1：统一账号、目录与属性读取

**状态：实现已交付，现有账号与在线摄像头联合通路验收通过；异常生命周期核验完成，跨账号替换实机保留未验证。** 同一 MiCloud 扫码账号完成恢复、84 台设备目录同步及 4 台代表设备的 13 项正式内部属性读取，全部 `code=0`；未增加独立 OAuth、uid 映射或额外授权页面。C700 与 C500 两镜头同时出帧，C500 两路共用一个物理连接，观看及后台媒体资源释放通过。目录离线摄像头未验证。只执行本任务，不进入 1.2。

[阶段入口](README.md) · 下一任务：[1.2 属性与在线推送](02-mqtt-push.md)

## 开始前

- 遵守仓库 AGENTS.md，读取 AGENTS.local.md，按其中路径只读核对 MiLoCo。
- 阅读 [共同参考](reference.md) 的当前扫码会话接入、属性返回码、交付位置、来源能力矩阵、调用范围与验收入口、验证交接；阅读 [总览共同检查](../../backend-household-perception.md#每步共同检查)。
- 遵守阶段入口的[账号接入前提](README.md#账号接入前提)：复用已有扫码登录、加密会话及恢复续期机制，不新增独立 OAuth 或额外授权入口。这是整组任务的前提，不只适用于本次属性读取。
- 前置基础是现有设备目录及规格查询；新增读取直接使用当前已登录账号。

## 本次范围

按 [目录架构](architecture.md) 组织 account、devices、properties、media 和 protocols/micloud；同批更新实际调用方、共享契约与文档，保留唯一账号所有者和唯一设备目录。

通过现有 MiCloud 实例交付 `readProperties(properties, signal)`。请求使用同一扫码账号的 Cookie、serviceToken、ssecurity 与 RC4 编码，访问 `/miotspec/prop/get`；读取编排、规格预检和来源配置位于 properties。任务 1.1 不实现 MQTT，也不预造 MQTT 凭据或占位接口。

## 实施清单

- [x] 按架构文件重组 account、devices、media、protocols/micloud；保留一个账号所有者和一个业务设备目录，删除原路径和转发别名，不新增通用 provider 层。
- [x] 根据操作者授权，从当前账号完整同步的目录选择代表设备及 readable 属性；本机记录地址与拓扑依据，未知网关、固件保持未知。订阅／在线能力记未验证，独立设备事件记未接入。
- [x] `MiCloud.getProperties` 复用现有扫码实例的认证与 RC4 请求；依据固定上游 `MiCloud.js` 的 `miotGetProps` 接入 `/miotspec/prop/get`，不创建独立 OAuth 或账号映射。
- [x] 账号所有者统一恢复、续期、保存及撤销扫码会话；属性读取捕获当前实例与采集代次，账号替换、会话失效、退出及关闭均使旧请求失效。
- [x] 交付正式内部 `readProperties(properties, signal)`，只接受当前账号目录中的设备及 readable 规格；不新增属性读取授权 UI 或临时 HTTP 路由。
- [x] 所有调用共用串行批次，应用预算为每批最多 150 项、单次 HTTP 最长 30 秒，支持取消，不隐式重试。按稳定来源遵守 `Retry-After`，期限内后续批次和新读取直接返回未发送观测，期限结束后等待下一次显式读取。账号自动及手动恢复续期也遵守最早重试时间，超长等待分段计时。逐项按 [返回码规则](reference.md#属性读取返回码) 处理，只接收本批地址，缺失 value 不补 null，部分失败不清掉成功项。
- [x] `datasource=1` 采用缓存优先策略，缓存缺失可能触发设备 RPC，不能保证最新值；规范化结果保守标为 `baseline/cloud_cache`、`observed_at=null`，保留 `read_started_at/received_at`，不提供周期轮询。

`mijia/service.ts` 负责账号所有权、凭据提交和读取资格；`account/maintenance.ts` 管理 MiCloud 恢复续期任务，`account/session.ts` 准备候选会话。家庭／规格查询位于 `devices/queries.ts`；属性请求位于 `protocols/micloud/client.ts`，属性地址类型和应用预算位于 `protocols/micloud/properties.ts`；规格预检、批次读取及来源配置位于 `properties/read-request.ts`、`properties/reader.ts`、`properties/source-profiles.ts`。协议、能力矩阵与真实限制见[米家来源契约](../../../mijia-source-contract.md)。实现勾选不代替下列实机验收。

## 验收与交接

当前联合验收证据位于 `data/verification/household-access/5e3c0f53-522e-49b0-a9ad-d3bc640a4bee/`，总体结论见 `report.json`。正式 `MijiaService` 从原加密扫码授权恢复账号；在同一服务实例与账号中完成设备目录、4 台代表设备规格查询和 `readProperties` 的 13 项读取，全部成功。现有扫码入口的二维码生成／取消通过，原账号和媒体代次不变；未重新执行用户扫码确认或账号替换，不把本项结论扩展为这些场景已通过。

在线摄像头范围为 C700 与 C500：三路同时持续解码超过 74 秒，C700 为 1920×1080，C500 两镜头为 848×480，均 `readyState=4`。关闭 C500 第一路后第二路继续出帧，第一路重开成功；8 个观看预留均 DELETE 204，随后 GET 404。C500 两路共用 1 个物理 TCP 连接；后台正常关闭后，C500 和 C700 的已建立连接均归零，保存登录保留。第三台摄像头目录离线，未验证其播放。多开发实例仍分别占用设备连接，不能把本机共享连接等同于跨机器共享或无限并发。

Go 正式源文件静态检查、镜像构建、前后端类型检查、lint 和构建通过；未新增测试或兼容路径。账号替换、供应商异常返回、超时取消与退出登录等独立验收项按下列清单保留其实际状态。

镜头数采用小米官方 MiLoCo 固定版本的通道能力数据，后台通过 `channelCount` 传递给 Go；业务和 Go 层不按 C500 型号分支。七款双摄的能力声明与公开规格已交叉核对，本地 C500 双摄／C700 单摄播放及释放回归通过，证据见 `data/verification/c500-investigation/capability-report.json`。其他型号的能力识别不等于已经实机验证其取流。

- [x] 联合验证现有扫码登录、设备目录、规格、摄像头播放和释放；属性读取复用同一已登录账号，不破坏已有通路。
- [x] 通过正式内部方法读取所选代表属性，记录真实逐项结果和脱敏证据；4 台／13 项全部成功，不能泛化到其他设备。
- [x] 核验会话恢复续期、账号替换、普通失败负码与接受类负码、缺失 value、超时取消及退出清理；实机与实现审查分别记录，无法验证的项目不标通过。
- [x] 更新来源契约及矩阵；在 `data/verification/household-access/<run_id>/report.json` 保存 `task_id=1.1` 的报告。代表设备选择保存在同目录下的本机材料中，不把真实账号、设备 ID 或凭据写入共享文档。
- [x] 完成总览共同检查，交接实际代码入口、逐项验收结论和本机报告位置。不新增测试或兼容路径。

### 异常生命周期验收状态

本机证据见 `data/verification/household-access/24f6e35a-e467-424c-b35c-533aba3eb07a/report.json`、`operations.json` 和源码哈希清单。核验工作已完成，发现的退出清理及页面重试问题已修复并实测。此清单勾选表示逐项核验、修复与记录完成，不表示所有实机场景均通过。跨账号替换按操作者明确要求保留实机未验证；内部确定性分支依据总览共同检查接受实现审查。

| 项目                       | 实现审查                                                                     | 实机证据                                                                                                                            |
| -------------------------- | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 保存会话恢复               | 通过：候选认证、目录读取、持久化及任务身份检查                               | 通过：正式服务恢复原账号及 84 台设备                                                                                                |
| 活动会话续期               | 通过：校验身份、串行保存、替换实例及代次；失败分流和调度已审查               | 通过：正式续期流程真实换取凭据，旧实例拒用，续期后 4 项读取成功；自然定时触发及供应商认证拒绝／限流未实测                           |
| 账号替换                   | 通过：保存候选后清旧作用域、媒体与目录，实例及代次拒收迟到结果               | 未验证：操作者没有第二个账号；同账号续期不冒充跨账号扫码                                                                            |
| 普通失败负码               | 通过：整数负码排除接受类后为 failure，保留其他成功项                         | 通过：同批 2 项 -704042011 失败、4 项 code=0 成功                                                                                   |
| 接受类负码                 | 通过：-702000000／-702010000 仍须有合法 value；按确定性分支验收              | 未验证：没有对应真实供应商样本                                                                                                      |
| 缺失 value                 | 通过：value_missing 不补 null，整项缺失为 response_missing；按确定性分支验收 | 未验证：普通失败负码缺少 value 不替代非失败码缺值样本                                                                               |
| 超时                       | 通过：单次属性 HTTP 30 秒预算覆盖传输与响应体，无隐式重试                    | 部分验证：已发出的真实云请求由调用方 10ms 期限中止并返回 timeout；go2rtc 真实暂停触发默认 10 秒超时；云属性默认 30 秒自然超时未验证 |
| 调用取消及生命周期取消     | 通过：预检、队列、传输和返回前检查取消／代次                                 | 通过：正式读取取消、退出及关闭取消并发读取，取消后读取恢复；精确队列时点及迟到回调按实现审查                                        |
| 退出本地账号与持久授权清理 | 通过：删除成功后清内存，失败不报告成功                                       | 通过：授权缺失、账号 idle、目录清空、观看预留 GET 404                                                                               |
| 退出远端媒体清理及重试     | 通过：失败传播并保留媒体错误／adapter，不恢复已撤销账号                      | 通过：暂停 go2rtc 后退出返回 504；恢复后重试 200，旧会话 409；页面刷新保留错误，重试清理成功后提示消失                              |
| 正常关闭                   | 通过：停止维护与读取、释放媒体、保留保存授权                                 | 通过：三路实际播放后关闭，C700／C500 物理连接各从 1 降至 0，在途读取取消，保存授权保留                                              |

`logout` 仅在授权撤销与远端媒体清理均成功后返回成功。清理失败时账号保持退出，媒体保留错误和待清理适配器，登录页提供“重试清理”；刷新页面不会丢失错误。go2rtc 的 60 秒租约与 5 秒扫描提供最终回收机制，不替代清理成功确认。30 秒预算只约束单次属性 HTTP，不包含规格预检、排队或多批次总耗时。核验后正式后端恢复原账号，账号、媒体绑定和 84 台设备目录均就绪。

**完成条件：** 目录迁移与统一账号／设备接线完成，已有登录、目录和视频通过验证；同一 MiCloud 扫码会话的属性读取、恢复续期、取消及退出清理有实现和证据。MQTT 鉴权尚未核实是任务 1.2 的边界，后续任务同样必须复用已有扫码流程，不增加独立 OAuth 或额外授权；未通过的读取或视频场景保持未通过／未验证。
